for compilation just do "make"

biquad_run.c contains:
1) core biquad filtering (copied from original biqud.c from musicdsp.com )
2) incorporated test code which reads WAV file and call biquad, and writes back as WAV.


